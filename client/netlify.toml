[build]
  command = "npm run build"
  publish = "dist"

# Forcing a change for Git to recognize
[build.environment]
  NODE_VERSION = "18" # Or your preferred Node.js version

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
